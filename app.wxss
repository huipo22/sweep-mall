/**app.wxss**/

@import './dist/style/base.wxss';
.container {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
  /* padding: 200rpx 0; */
  box-sizing: border-box;
}

.Box {
  width: 100%;
}

/* 遮罩 */

.wrapper {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;
}

.block {
  width: 120px;
  height: 120px;
  background-color: #fff;
}

/* 弹出层 下方 */

.popup {
  display: flex;
  flex-direction: row;
  padding: 10rpx 20rpx;
}

.price {
  display: flex;
  align-items: flex-end;
  color: #ff7700;
}

.popup-title {
  display: flex;
  justify-content: center;
  align-items: center;
  /* font-size: .8rem; */
}

.popup-btn {
  width: 100%!important;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #f70;
  color: #fff;
}

.stepper {
  flex: 1;
  display: flex;
  flex-direction: row;
  justify-content: flex-end;
}

/* 页面边距 */

.pageSetting {
  padding: 0px 16px;
  box-sizing: border-box;
  background: #fff;
}

.Boxtop {
  margin-top: 16rpx;
}

.Boxpadding {
  padding: 20rpx;
  box-sizing: border-box;
}